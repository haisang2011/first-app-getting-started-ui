import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
  typography: {
    body2: {
      fontSize: '1.4rem',
    },
    h5: {
      fontSize: '1.6rem',
    },
    h4: {
      fontSize: '1.8rem',
      fontWeight: 500
    }
  },
});